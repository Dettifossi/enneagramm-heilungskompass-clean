# Übergabe-Notiz

**Projekt:** Enneagramm-Heilungskompass
**Pfad:** `~/Library/Mobile Documents/com~apple~CloudDocs/Enneagramm-Kompass`

## Stand (10.–11.08.2026)
- Neues Portrait „Baruch de Spinoza" (SE5w6, Eule) fertiggestellt: Seite, Bild, Register (DE+EN), Verlinkung — alles live und getestet.
- Portraits-Ordner aufgeräumt (Duplikate & `.DS_Store` entfernt, `.gitignore` ergänzt).
- Lokaler Testserver läuft immer auf Port 4174: `python3 -m http.server 4174`
- Es gibt noch uncommittete Änderungen (`git status`) u. a. in `ai-prototype/` — noch nicht besprochen/entschieden, was damit passieren soll.

## Offen / nächste Schritte
- Ggf. weitere neue Portraits (Muster: Bild in `assets/portraits/` legen, dann Seite + Register + Verlinkung nach dem Spinoza-Vorbild in `bundle.js`).
- Entscheidung zu den offenen `ai-prototype/`-Änderungen steht noch aus.
